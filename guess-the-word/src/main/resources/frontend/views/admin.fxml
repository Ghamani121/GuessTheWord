<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<TabPane fx:id="tabPane" xmlns:fx="http://javafx.com/fxml" fx:controller="com.guesstheword.frontend.controllers.AdminController">

    <tabs>

        <!-- Daily Report Tab -->
        <Tab text="Daily Report">
            <VBox spacing="10">
            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>

                <HBox spacing="10">
                    <Label text="Select Date:"/>
                    <DatePicker fx:id="dailyDatePicker"/>
                    <Button text="Generate Report" onAction="#generateDailyReport"/>
                </HBox>

                <GridPane hgap="10" vgap="10">
                    <Label text="Total Users:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
                    <Label fx:id="totalUsersLabel" GridPane.rowIndex="0" GridPane.columnIndex="1"/>
                    <Label text="Correct Guesses:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
                    <Label fx:id="correctGuessesLabel" GridPane.rowIndex="1" GridPane.columnIndex="1"/>
                </GridPane>

            </VBox>
        </Tab>

        <!-- User Report Tab -->
        <Tab text="User Report">
            <VBox spacing="10" >
                <padding>
                    <Insets top="20" right="20" bottom="20" left="20"/>
                </padding>

                <HBox spacing="10">
                    <Label text="Select User:"/>
                    <ComboBox fx:id="userComboBox"/>
                    <Label text="Select Date:"/>
                    <DatePicker fx:id="userDatePicker"/>
                    <Button text="Generate Report" onAction="#generateUserReport"/>
                </HBox>

                <GridPane hgap="10" vgap="10">
                    <Label text="Words Tried:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
                    <Label fx:id="wordsTriedLabel" GridPane.rowIndex="0" GridPane.columnIndex="1"/>
                    <Label text="Correct Guesses:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
                    <Label fx:id="userCorrectGuessesLabel" GridPane.rowIndex="1" GridPane.columnIndex="1"/>
                </GridPane>

            </VBox>
        </Tab>

    </tabs>
</TabPane>
